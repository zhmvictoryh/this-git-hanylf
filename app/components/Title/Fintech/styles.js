import { StyleSheet } from "react-native";

export default StyleSheet.create({
    titleList: {
        flexDirection: "row",
        justifyContent: "space-between",
        alignItems: "center",
        marginTop: 20,
        marginBottom: 10,
    },
});
