import { StyleSheet } from "react-native";

export default StyleSheet.create({
    title: {
        marginTop: 20,
        marginBottom: 10,
        fontWeight: "bold",
        fontSize: 20,
    },
});
