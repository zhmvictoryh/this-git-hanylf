import { StyleSheet } from "react-native";

export default StyleSheet.create({
    headerView: {
        marginTop: 20,
        justifyContent: "center",
        alignItems: "center",
        paddingBottom: 60,
    },
});
